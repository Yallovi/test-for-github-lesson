/**
 * 7.6 Git – Слияние – Создание коммита из ветки: merge --squash
 *
 * Например, у нас есть отдельная ветка и мы хотим взять результаты из нее, но не объединять, а одним комитов перенести
 * все изменения
 *
 * Для этого есть флаг squash
 *
 */
