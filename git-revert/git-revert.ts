/**
 * * git revert (revert - возвращаться)!!!!!
 *
 * @format
 */

/**
 * * Например, мы знаем, какой коммит крашит наше приложение и нужно его убрать.
 * * git revert <хэш, в котором ошибка> - не удаляет из истории коммита, а добавляет еще один коммит поверх, но без изменений, которые находятся в коммите с ошибкой
 */

/**
 * * Например, мы сделали git add .  , но хотим отменить изменения только в дух файлах Readme.md, second.ts
 * * git checkout Readme.md second.ts
 */
