/**
 * git rm - удаление файла из рабочей директории и добавление сразу в индекс
 * * флаг -r - если нужно удалить папку
 * * флаг --cached - удалит из индекса, но оставит в рабочей
 *
 * git rm -r file = rm -r file + git add file
 *
 */
