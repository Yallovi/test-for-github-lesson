/**
 * git reset --mixed или смешанные reset
 *
 * Флаг --mixed - идет по умолчанию его можно не прописывать.
 * Флаг --mixed переводит HEAD на указанный коммит и помещает изменения в рабочую директорию
 */
