/**
 * Git – Reset – Мягкий reset --soft: замена и объединение коммитов
 *
 * Переводит указатель HEAD на коммит который мы указали, а изменения помещает в index
 * git reset --soft <id commit>
 *
 * Можно представить, что данная ситуация выглядит так:
 *
 * 1. Мы сделали изменения
 * 2. git add .
 * 3. но не сделали коммит
 *
 */
