/**
 * * git rebase -i HEAD~3 (считается с первого) (интерактивный rebase)
 * * Например если мы хотим  изменить историю коммитов
 *
 * @format
 */

/**
 * * Если эта ветка с коммитами уже была в удаленном репозитории, при git rebase - i мы меняем хэш коммитов и чтоб запушить нужно сделать
 * ! git push origin <name_branch> --force
 */
