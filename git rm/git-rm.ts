/**
 * * Для удаления файла с помощью команды
 * * git rm <paths> = rm + git add
 * * Флаги:
 * * -r : git rm -r - для удаления директории
 * * -f - чтоб гит игнорировал предупреждения
 *
 * @format
 */
