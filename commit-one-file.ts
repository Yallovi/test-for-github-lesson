/**
 * Если у нам нужно закомитить один файл, а в индексе уже лежит что-то
 * чтоб не откатывать можно выполнить следующую команду
 *
 * git commit -m "Message" .file
 */

/**
 * Но это работает только если это не новый
 */
